[global]
# TraceISO's published dependencies are all available from the official index.
# An empty value prevents stale machine-wide extra indexes from delaying setup.
index-url = https://pypi.org/simple
extra-index-url =
