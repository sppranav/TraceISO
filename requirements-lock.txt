# Exact pins for the listed application dependencies, not a fully resolved lock.
# A successful release test run using these pins has not been established.
# Transitive dependencies are not pinned; cross-platform compatibility is unverified.
# See README.md, "Python compatibility", for the supported scope.
# PyQt desktop utilities are intentionally excluded; install
# `.[tools]` separately when those optional utilities are needed.
numpy==2.4.6
pandas==3.0.5
scipy==1.17.1
h5py==3.16.0
jinja2==3.1.6
plotly==7.0.0
streamlit==1.62.0
openpyxl==3.1.5
kaleido==1.3.0
watchdog==6.0.0
packaging==26.3
